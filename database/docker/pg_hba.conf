# =============================================
# FILE: docker/pg_hba.conf
# PURPOSE: قواعد الوصول لقاعدة البيانات
# =============================================

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# الاتصالات المحلية
local   all             all                                     trust

# الاتصالات من الشبكة الداخلية
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5

# الاتصالات من شبكة Docker
host    all             all             172.16.0.0/12           md5
host    all             all             192.168.0.0/16          md5

# الوصول للإدارة
host    postgres        postgres        0.0.0.0/0               reject
host    postgres        all             0.0.0.0/0               reject
